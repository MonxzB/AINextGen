import Link from "next/link";
import { login } from "./actions";
import { ThemeToggle } from "@/components/theme-toggle";
import { BrandLogo } from "@/components/brand-logo";
export default async function LoginPage({searchParams}:{searchParams:Promise<{error?:string}>}){const {error}=await searchParams;return <main className="relative grid min-h-screen place-items-center bg-gradient-to-br from-brand-50 to-amber-50 p-4"><ThemeToggle className="absolute right-4 top-4"/><div className="card w-full max-w-md p-8"><Link href="/" className="inline-flex"><BrandLogo/></Link><h1 className="mt-8 text-3xl font-black">Đăng nhập quản trị</h1><p className="mt-2 text-sm text-black/50">Quản lý và xuất bản nội dung kiến thức AI.</p>{error&&<p className="mt-5 rounded-xl bg-red-50 p-3 text-sm text-red-700">{error}</p>}<form action={login} className="mt-6 space-y-4"><label className="block text-sm font-bold">Email<input className="input mt-2" type="email" name="email" autoComplete="email" required/></label><label className="block text-sm font-bold">Mật khẩu<input className="input mt-2" type="password" name="password" autoComplete="current-password" required minLength={6}/></label><button className="btn-primary w-full">Đăng nhập</button></form></div></main>}
