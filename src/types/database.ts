export type Category = { id: string; name: string; slug: string; description: string | null; image_url: string | null; is_active: boolean; created_at: string };
export type Brand = { id: string; name: string; slug: string; description: string | null; logo_url: string | null; is_active: boolean; created_at: string };
export type Product = { id: string; category_id: string | null; brand_id: string | null; name: string; slug: string; short_description: string | null; description: string | null; thumbnail_url: string | null; rating: number; review_count: number; is_featured: boolean; is_active: boolean; seo_title: string | null; seo_description: string | null; created_at: string; category?: Pick<Category,"name"|"slug"> | null; brand?: Pick<Brand,"name"|"slug"> | null; affiliate_links?: AffiliateLink[] };
export type AffiliateLink = { id: string; product_id: string; marketplace_id: string; url: string; price: number | null; original_price: number | null; seller_name: string | null; warranty: string | null; shipping_info: string | null; voucher_info: string | null; product_condition: string; note: string | null; is_official_store: boolean; is_active: boolean; last_checked_at: string | null; marketplace?: { name: string; logo_url: string | null } | null };
