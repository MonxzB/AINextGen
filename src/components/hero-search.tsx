"use client";
import { useEffect, useState } from "react";
import { Search } from "lucide-react";
const samples={vi:["Prompt Engineering cho người mới","Tự động hóa công việc với AI","Lộ trình học AI trong 30 ngày","Tạo hình ảnh bằng AI"],en:["Prompt Engineering for beginners","Automate work with AI","30-day AI learning roadmap","Create images with AI"]};
export function HeroSearch({locale="vi"}:{locale?:"vi"|"en"}){const phrases=samples[locale];const en=locale==="en";const [sampleIndex,setSampleIndex]=useState(0);const [typed,setTyped]=useState("");const [deleting,setDeleting]=useState(false);const [focused,setFocused]=useState(false);const [reduceMotion,setReduceMotion]=useState(false);useEffect(()=>{const media=window.matchMedia("(prefers-reduced-motion: reduce)");const update=()=>setReduceMotion(media.matches);update();media.addEventListener("change",update);return()=>media.removeEventListener("change",update)},[]);useEffect(()=>{if(focused||reduceMotion)return;const sample=phrases[sampleIndex];let delay=deleting?35:75;if(!deleting&&typed===sample){delay=1500}else if(deleting&&typed===""){delay=350}const timer=window.setTimeout(()=>{if(!deleting&&typed===sample){setDeleting(true)}else if(deleting&&typed===""){setDeleting(false);setSampleIndex(index=>(index+1)%phrases.length)}else{setTyped(deleting?sample.slice(0,Math.max(0,typed.length-1)):sample.slice(0,typed.length+1))}},delay);return()=>window.clearTimeout(timer)},[deleting,focused,phrases,reduceMotion,sampleIndex,typed]);const animated=reduceMotion?phrases[0]:typed;return <form action={en?"/en/search":"/search"} role="search" className="hero-search mt-8 flex max-w-xl gap-2 rounded-2xl bg-white p-2 shadow-soft"><label className="relative min-w-0 flex-1"><span className="sr-only">{en?"Search AI knowledge":"Tìm kiến thức AI"}</span><Search className="pointer-events-none absolute left-3 top-1/2 -translate-y-1/2 text-black/40" size={21}/><input name="q" type="search" className="h-12 w-full bg-transparent pl-11 pr-2 text-ink outline-none placeholder:text-black/45" placeholder={focused?(en?"Enter what you want to find...":"Nhập từ khóa bạn muốn tìm..."):`${animated}${reduceMotion?"":"▌"}`} onFocus={()=>setFocused(true)} onBlur={()=>setFocused(false)} autoComplete="off"/></label><button className="btn-primary shrink-0">{en?"Explore":"Khám phá"}</button></form>}
